
let NFTCollection = [];

function mintNFT(name, artist, description, yearCreated) {
    let nft = {
        name: name,
        artist: artist,
        description: description,
        yearCreated: yearCreated
    };
    
    NFTCollection.push(nft);
    console.log(`Minted NFT: ${name}`);
}

function listNFTs() {
    NFTCollection.forEach((nft, index) => {
        console.log('\n');
        console.log(`NFT #${index + 1}`);
        console.log(`Name: ${nft.name}`);
        console.log(`Artist: ${nft.artist}`);
        console.log(`Description: ${nft.description}`);
        console.log(`Year Created: ${nft.yearCreated}`);
        console.log('---------------------------');
    });
}

function getTotalSupply() {
    console.log(`Total NFTs minted: ${NFTCollection.length}`);
}

mintNFT("Bangus Festival", "Belen", "A Bangus Festival held in Dagupan to celebrate their culture.", 2023);
mintNFT("Pinoy Rap", "Shanti Dope", "A hit collections of Filipino Rapper.", 2022);
mintNFT("Fireworks", "Russel", "A colorful and outstanding play to light up the sky.", 2024);

listNFTs();
getTotalSupply();
